<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:layout_margin="10dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:cardUseCompatPadding="false"
    app:strokeColor="@android:color/transparent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="14dp"
        android:paddingEnd="14dp">

        <!-- left icon -->
        <ImageView
            android:id="@+id/icon"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:contentDescription="@null" />

        <!-- title -->
        <TextView
            android:id="@+id/title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:paddingStart="12dp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/GoldenEmber"
            android:textSize="16sp"
            android:fontFamily="@font/aref_ruqaa"/>

        <!-- reward chip (shown when NOT completed) -->
        <TextView
            android:id="@+id/reward"
            android:layout_width="wrap_content"
            android:layout_height="28dp"
            android:minWidth="56dp"
            android:gravity="center"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:textColor="@color/GoldenEmber"
            android:textSize="14sp"
            android:fontFamily="@font/aref_ruqaa"
            android:background="@drawable/bg_reward_chip"
            android:visibility="gone"/>

        <!-- golden tick (shown when completed) -->
        <ImageView
            android:id="@+id/tick"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_tick_filled"
            app:tint="@color/GoldenEmber"
            android:contentDescription="@null"
            android:visibility="gone"/>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
