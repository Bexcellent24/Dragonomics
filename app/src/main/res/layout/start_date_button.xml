<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/startDateButtonRoot"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/date_button_background"
    android:padding="2dp"
    android:clickable="true"
    android:focusable="true"
    android:layout_margin="2dp">

    <TextView
        android:id="@+id/startDateText"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:text="Start Date"
        android:textColor="@color/GoldenEmber"
        android:gravity="center"
        android:textSize="12sp"
        android:fontFamily="@font/aref_ruqaa"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/startDateButtonIcon"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <ImageView
        android:id="@+id/startDateButtonIcon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/calandar"
        android:scaleType="centerInside"
        android:layout_margin="2dp"
        android:contentDescription="Select Date"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>