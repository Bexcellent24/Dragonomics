<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/shopCard"
    android:layout_width="wrap_content"
    android:minWidth="350dp"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:layout_margin="24dp"
    android:backgroundTint="#142431"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Header row with currency and close button -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp">

            <!-- Currency (left) -->
            <LinearLayout
                android:id="@+id/shopCurrencyRow"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentStart="true"
                android:layout_centerVertical="true"
                android:orientation="horizontal">

                <ImageView
                    android:id="@+id/shopCurrencyIcon"
                    android:layout_width="30dp"
                    android:layout_height="30dp"
                    android:layout_marginTop="2dp"
                    android:layout_marginEnd="6dp"
                    android:contentDescription="Currency"
                    android:scaleType="fitCenter"
                    android:src="@drawable/currency" />

                <TextView
                    android:id="@+id/shopCurrencyAmount"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="@font/aref_ruqaa"
                    android:text="0"
                    android:textColor="@color/GoldenEmber"
                    android:textSize="18sp" />
            </LinearLayout>

            <!-- Close button (right) -->
            <ImageButton
                android:id="@+id/shopCloseX"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:background="@android:color/transparent"
                android:contentDescription="Close Shop"
                android:scaleType="fitCenter"
                android:src="@drawable/close_cross"
                app:tint="@color/GoldenEmber" />
        </RelativeLayout>

        <!-- Title (centered) -->
        <TextView
            android:id="@+id/shopTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/breathe_fire_iii"
            android:gravity="center"
            android:text="Dragon Shop"
            android:textColor="@color/GoldenEmber"
            android:textSize="24sp"
            android:layout_marginBottom="12dp" />

        <!-- Tabs row -->
        <com.google.android.material.tabs.TabLayout
            android:id="@+id/shopTabs"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/DeepAbyss"
            app:tabIndicatorColor="@color/GoldenEmber"
            app:tabIndicatorFullWidth="false"
            app:tabIndicatorHeight="3dp"
            app:tabRippleColor="@android:color/transparent"
            app:tabSelectedTextColor="@color/GoldenEmber"
            app:tabTextColor="@color/GoldenEmber" />

        <!-- Grid with items -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/shopRecycler"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:clipToPadding="false"
            android:padding="12dp" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>